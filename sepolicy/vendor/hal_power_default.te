r_dir_file(hal_power_default, debugfs_wlan)

r_dir_file(hal_power_default, sysfs_touchpanel)
allow hal_power_default sysfs_touchpanel:file write;

# Allow writing to files in sysfs
allow hal_power_default sysfs:file rw_file_perms;
